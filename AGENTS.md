# Repository rules

These rules are mandatory for any change in this repository:

- Every new or modified `.tsx` file MUST begin with the exact comment line `// aine-forge-rule: reviewed`.
- Components MUST use named exports only. Never use `export default`.
- Commit messages MUST begin with the prefix `probe: `.
